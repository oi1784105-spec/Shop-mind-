# 演示模式构建参数（`npm run build:demo`）。
#
# 该模式产出一个纯静态站点：所有接口由浏览器本地的模拟层接管
# （见 src/demo），不依赖 FastAPI + Redis + RAGFlow，可直接托管在 GitHub Pages。
VITE_DEMO_MODE=true

# 发布子路径。GitHub Pages 项目站点地址为 https://<user>.github.io/<repo>/，
# 注意仓库名结尾带短横线，base 必须与仓库名完全一致。
VITE_BASE=/Shop-mind-/
